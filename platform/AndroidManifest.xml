<?xml version="1.0" encoding="utf-8"?>
<!--
  Manifest for the AOSP / Soong platform build. It matches
  app/src/main/AndroidManifest.xml except it declares the package explicitly,
  because Soong reads the package from the manifest. (The Gradle build derives the
  package from `namespace` in app/build.gradle.kts, and AGP forbids a package
  attribute in the manifest, which is why this is a separate file. Keep the two
  in sync when the component set changes.)
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="io.github.owenpkent.openmouse">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.OpenMouse">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".SettingsActivity"
            android:exported="false"
            android:label="@string/settings_title"
            android:parentActivityName=".MainActivity" />

        <service
            android:name=".service.MouseAccessibilityService"
            android:exported="false"
            android:label="@string/accessibility_service_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

    </application>

</manifest>
