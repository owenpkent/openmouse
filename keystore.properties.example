# Copy to keystore.properties (which is gitignored) and fill in to sign a
# release build. F-Droid signs with its own key, so this is only needed for
# your own signed APKs / Google Play uploads.
storeFile=/absolute/path/to/openmouse-release.jks
storePassword=changeme
keyAlias=openmouse
keyPassword=changeme
