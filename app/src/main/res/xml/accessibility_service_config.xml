<?xml version="1.0" encoding="utf-8"?>
<!--
    canPerformGestures="true" is what allows the service to call
    dispatchGesture(), the API used to inject taps and swipes at the cursor.

    OpenMouse only injects gestures and draws an overlay; it does not read window
    content or react to UI events, so canRetrieveWindowContent and a broad event
    mask are intentionally omitted (they would add an alarming enable prompt and
    needless event traffic). Mouse observation on API 34+ is configured at
    runtime via FLAG_SEND_MOTION_EVENTS + setMotionEventSources.

    flagRequestTouchExplorationMode is intentionally NOT set: we want raw pointer
    input, not touch-exploration semantics.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagDefault"
    android:canPerformGestures="true"
    android:description="@string/accessibility_service_description"
    android:notificationTimeout="100"
    android:settingsActivity="io.github.owenpkent.openmouse.SettingsActivity" />
